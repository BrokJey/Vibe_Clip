<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VibeClip.Splash" parent="Theme.VibeClip_Frontend">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Не показывать круглую системную иконку — только наш логотип из windowBackground -->
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_empty</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>
